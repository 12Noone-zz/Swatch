<div class="comments">
	<% @project.comments.each do |comment| %>
		<div class="comment">
			<% if comment.body %>
				<p><%= current_user.name %>
				<p><%= comment.body %></p>
				<i><%= comment.created_at %></i>
			<% end %>
		</div>
	<% end %>
</div>