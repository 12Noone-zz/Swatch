<div class="comments">
	<% @project.comments.each do |comment| %>
		<div class="comment">
			<% if comment.body %>
				<p><%= current_user.name %>
				<p><%= comment.body %></p>
				<em><%= comment.created_at %></em>
				<hr>
			<% end %>
		</div>
	<% end %>
</div>